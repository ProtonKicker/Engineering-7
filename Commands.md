# Linear Algebra

np.matrix\_rank(A)
np.hstack((A, b))
np.pinv(A)
A @ b



# Randomness (scipy.stats)

stats.rv\_discrete(values=(X, p))
stats.uniform(loc=a, scale=h)
stats.norm(loc=mu, scale=sigma)
X.rvs(size=n)
X.rvs(size=n, random\_state=s)

